# BCCT 點餐表 使用說明

`order.html` 是**一頁多餐廳**的點餐表。你（主辦方）用網址參數決定：
1. 顯示哪一家餐廳的菜單（客戶不能改）
2. 這次是哪一間客戶公司／活動（會直接當成 Google Sheet 的分頁名稱）

---

## 網址怎麼組

```
https://你的網址/order.html?r=餐廳代號&a=公司或活動名稱
```

| 參數 | 意思 | 可填的值 |
|------|------|----------|
| `r` | 餐廳（決定菜單、飲料、是否有照片） | `25cafe` = 森入心流<br>`arcadia` = 花岩山林 |
| `a` | 客戶公司／活動名稱 | 任意文字，例如 `ABC公司-9月團購`。<br>**這個字串會變成 Google Sheet 的分頁名稱**，同名就會寫進同一頁。 |

> `r` 沒帶時預設為 `25cafe`（相容舊連結）；舊的 `?r=senru` 也還能用。`a` 沒帶時表單會鎖住並顯示提醒。

### 範例

- 花岩山林、客戶「大樹科技」10 月活動：
  ```
  order.html?r=arcadia&a=大樹科技-2025-10
  ```
- 森入心流、客戶「晴天顧問」尾牙：
  ```
  order.html?r=25cafe&a=晴天顧問-尾牙
  ```

---

## 資料會進到哪裡

- 全部寫進**跟原本 order.html 同一個 Google Sheet**
  （Apps Script `ORDER_SHEET_ID` = `1XkoxvJfl4fhp4lTBjVHqkY29ixPJZGrEeiF9UoZQqwg`）。
- 依 `a` 參數自動建立／寫入對應**分頁**。第一次出現的名稱會自動開新分頁並加標題列。
- 欄位：`送出時間 / 活動 / 姓名 / 餐點 / 價格 / 飲料`。
  花岩山林每人價格固定寫 `400`（客戶點餐頁看不到，只寫進 sheet）；森入心流依所選餐點帶價格。
- 建議 `a` 用「公司-活動-年月」這種好認又不重複的命名，避免兩個活動誤寫同一分頁。

---

## 各餐廳目前的內容

### 森入心流 `r=25cafe`
- 菜單：文字下拉，排餐 7 種 + 燉飯／義大利麵 12 種，有價格
- 飲料（7 選 1）：紅茶冰 / 綠茶冰 / 美式冰 / 蜂蜜水 / 紅茶熱 / 綠茶熱 / 美式熱

### 花岩山林 `r=arcadia`
- 菜單：**照片卡片點選**，5 種
  - 墨魚麵 / 嫩煎松阪豬燉飯 / 嫩煎雞腿排蕃茄義大利麵 / 惡魔風辣味雞腿腿炒飯 / 牛肝菌時蔬燉飯
- 飲料（5 選 1）：咖啡冰 / 咖啡熱 / 紅茶冰 / 紅茶熱 / 果汁
- 價格：客戶頁不顯示，但 sheet 會固定寫每人 `400`。要改金額改 `order.html` 裡 arcadia 的 `fixedPrice`。
- 照片放在 `花岩山林餐點照片/` 資料夾，**部署時要跟 order.html 一起上傳**，否則照片會破圖。

---

## 之後要加第三家餐廳怎麼做

打開 `order.html`，找到 `const RESTAURANTS = {` 這段，照 `arcadia` 的格式多加一組即可，例如：

```js
newname: {
  label: '新餐廳名稱',
  mealMode: 'photos',        // 'photos' = 照片卡片；'dropdown' = 文字下拉
  showPrice: false,
  photoDir: '新餐廳照片資料夾',
  meals: [
    { name: '餐點A', img: '餐點A.jpg' },
    { name: '餐點B', img: '餐點B.jpg' },
  ],
  drinks: ['冰紅茶', '熱紅茶', '果汁'],
},
```

若是文字下拉（像森入心流），改用：

```js
newname: {
  label: '新餐廳名稱',
  mealMode: 'dropdown',
  showPrice: true,
  mealGroups: [
    { label: '分類名稱', items: [ ['餐點A', 300], ['餐點B', 350] ] },
  ],
  drinks: ['冰紅茶', '熱紅茶'],
},
```

再到 `R_ALIASES` 加一行 `newname: 'newname',`（要中文代號也可加 `'新餐廳名稱': 'newname',`），
分享時網址用 `?r=newname&a=公司名稱`。

菜單／飲料以外都不用動，Google Apps Script 也不用改。
